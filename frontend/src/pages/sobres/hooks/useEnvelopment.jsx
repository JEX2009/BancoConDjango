import { useState } from "react";
import { authService } from "../../../api/UserService";
import useCreate from "../../../hooks/useCreate";

export default function useLogi(){
    const {dato, cargando, error, exito, post } = useCreate(authService.);

    const POSTSobre = (datos)=>{
        useCreate
    };
}